# 2026-04 合并计划

## 月度结论

2026-04 基本跳过。该月提交都是 `create-unibest` CLI 的 Wot UI 2 支持、提示和元数据。当前 app 已完成 Wot UI 2.x 适配，不需要引入这些 CLI 改动。

## commit 分类

| 类型 | commit | 处理建议 |
| --- | --- | --- |
| 完全合并 | 无 | 无。 |
| 部分合并 | 无 | 无。 |
| 忽略 | `8b66249` | CLI 支持 `@wot-ui/ui`，当前 app runtime 不需要。 |
| 忽略 | `93f1a35` | CLI 自动注入 wot-ui-v2 resolver；当前 `vite.config.ts` 已有本地 Wot UI 2 resolver。 |
| 忽略 | `05fe269` | CLI package 元数据。 |
| 忽略 | `6450aab` | CLI 创建后 AI 文档提示。 |

## 备注

如果未来要维护自己的 `create-unibest` 脚手架，再回看这一月；当前 Yudao app 不处理。
