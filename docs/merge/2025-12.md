# 2025-12 合并计划

## 月度结论

2025-12 适合只拿“依赖 + tabbar + 初始化脚本”相关修复，不适合按月整体合并。

## commit 分类

| 类型 | commit | 处理建议 |
| --- | --- | --- |
| 完全合并 | 无 | 无低风险整 commit。 |
| 部分合并 | `61cceb5` | `@uni-helper` 相关依赖升级，需结合当前 subPackages、Wot UI 2 resolver 测试。 |
| 部分合并 | `bffa3bd` | tabbar 策略收敛，当前项目 tabbar 已定制，只能抽思路。 |
| 部分合并 | `8926504` | 抽 `TabbarItem` 的结构参考价值有限；当前 `index.vue` 仍是单文件实现，默认不急。 |
| 部分合并 | `36abf49`、`a75502a` | Vite 插件顺序/排除规则，需适配当前 `src/pages-core`、`src/pages-system`、`src/pages-infra`、`src/pages-bpm` 分包。 |
| 部分合并 | `bfe5c04`、`a487ba7` | `create-base-files` 修复，可在需要处理 pages.json 初始化问题时抽取。 |
| 部分合并 | `4fa7a43` | Vite 插件依赖升级，必须配合 lockfile 和构建验证。 |
| 忽略 | `cc8db0d`、`286f23e`、`ff5ef61`、`8a20eec` | 上游版本号更新，不适合覆盖当前项目。 |
| 忽略 | `1021b55`、`4ebed7a`、`b9a3bf2`、`bd21100`、`b2a3496`、`a068049`、`a09365f` | 上游文档/注释更新。 |
| 忽略 | `2939eb8` | 远端合并提交。 |
| 忽略 | `24a6712` | VSCode snippets，不是运行时修复。 |
| 忽略 | `0f68e06` | package script 顺序整理，当前不需要。 |

## 实施口径

本月不要做一整批。只有在后续遇到对应问题时，单独抽：

- `@uni-helper` 依赖升级。
- Vite 插件顺序和 `exclude` 规则。
- `create-base-files` 对 pages.json 的修复。

## 验证要求

- `pnpm type-check`
- `pnpm build:h5`
- `pnpm build:mp-weixin`
- 如果动了 `pages.config.ts` / `vite.config.ts`，必须检查生成的 `src/pages.json` 和分包配置。
