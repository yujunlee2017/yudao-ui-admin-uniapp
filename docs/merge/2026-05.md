# 2026-05 合并计划

## 月度结论

2026-05 分两段做：

- 先做低风险项：baseUrl、dev-tools、import-sort、`tabBar` fallback、native plugin copy 日志。
- tabbar 同步修复单独作为高风险专项，不能放第一轮。

## 前置条件

当前仓库没有这些提交对象。实施前需要先 fetch `uni-run/unibest`：

```bash
git remote add unibest https://github.com/uni-run/unibest.git
git fetch unibest main
```

## commit 分类

| 类型 | commit | 处理建议 |
| --- | --- | --- |
| 完全合并 | `278610a`、`d1898f2` | 微信开发者工具 CLI 路径配置。 |
| 完全合并 | `081a20c` | 禁用 import 自动排序，避免破坏 uni 条件编译。 |
| 完全合并 | `1a1e253` | `tabBar` fallback `{}`。 |
| 部分合并 | `1c6a48d` | 微信小程序 baseUrl env 化，需适配当前 env。 |
| 部分合并 | `4024e1f` | 只拿 native plugin copy 日志，不拿默认开启。 |
| 部分合并 | `03e115f`、`eab2e80`、`ce8ed97` | tabbar 同步修复，高风险，单独专项。 |
| 忽略 | `1678b20` | CLI add 参数。 |
| 忽略 | `60f8b18` | 上游分支工作流文档。 |

## 低风险项清单

### `1c6a48d` 微信小程序 baseUrl

需要提取：

- `env/.env`
  - 增加可选配置注释：`VITE_SERVER_BASEURL__WEIXIN_DEVELOP`、`VITE_SERVER_BASEURL__WEIXIN_TRIAL`、`VITE_SERVER_BASEURL__WEIXIN_RELEASE`。
  - 保留当前 `VITE_SERVER_BASEURL = 'http://localhost:48080/admin-api'` 作为默认地址。
- `src/env.d.ts`
  - 增加三个可选 env 类型声明。
- `src/utils/index.ts`
  - 删除当前硬编码的 `VITE_SERVER_BASEURL__WEIXIN_DEVELOP/TRIAL/RELEASE = 'http://localhost:48080/admin-api'`。
  - 改为从 `import.meta.env` 读取微信小程序专用地址。
  - 没有配置专用地址时，回退到 `VITE_SERVER_BASEURL`。

验证：

- H5 仍使用 `VITE_SERVER_BASEURL`。
- 微信小程序 develop / trial / release 三种 `envVersion` 都能按配置取专用地址；未配置时回退默认地址。

### `278610a`、`d1898f2` dev-tools CLI path

需要提取：

- `env/.env`
  - 增加 `WECHAT_DEVTOOLS_CLI_PATH` 注释示例，不强制写死值。
- `vite.config.ts`
  - 使用 `loadEnv(mode, envDir, '')` 读取非 `VITE_` 前缀的 `WECHAT_DEVTOOLS_CLI_PATH`。
  - 调用 `openDevTools({ mode, wechatDevtoolsCliPath: WECHAT_DEVTOOLS_CLI_PATH })`。
- `scripts/open-dev-tools.js`
  - 支持通过传参覆盖微信开发者工具 CLI 路径。
  - 打开失败时打印当前 CLI 命令和配置提示。

验证：

- 不配置 `WECHAT_DEVTOOLS_CLI_PATH` 时使用默认路径。
- 配置 `WECHAT_DEVTOOLS_CLI_PATH` 后使用配置路径。

### `081a20c` import-sort

需要提取：

- `eslint.config.mjs`
  - 关闭会破坏 uni 条件编译 import 边界的 import 自动排序规则。

验证：

- 条件编译包裹 import 的文件不会被 lint fix 重排到错误平台边界外。

### `1a1e253` tabBar fallback

需要提取：

- `src/tabbar/config.ts`
  - `export const tabBar = tabbarCacheEnable ? _tabbar : {}`。

验证：

- `NO_TABBAR` 场景下 `pages.config.ts` 不拿到 `undefined` tabbar。

### `4024e1f` native plugin copy 日志

明确操作：只拿日志优化，不拿默认开启。

需要提取：

- `vite-plugins/copy-native-resources.ts`
  - 成功日志打印 `sourcePath -> targetPath`。
  - 保留复制文件/目录数量日志。
- `env/.env`
  - 保持 `VITE_COPY_NATIVE_RES_ENABLE = false`。
- `vite.config.ts`
  - 保持 `UNI_PLATFORM === 'app' && VITE_COPY_NATIVE_RES_ENABLE === 'true'`。

验证：

- `VITE_COPY_NATIVE_RES_ENABLE=false` 时 App 构建不复制 nativeplugins。
- `VITE_COPY_NATIVE_RES_ENABLE=true` 且 `UNI_PLATFORM=app` 时才复制，并输出路径日志。

## tabbar 同步专项

涉及：`03e115f`、`eab2e80`、`ce8ed97`。

这是高风险项，必须单独做。

结构差异和风险：

- 上游 `src/tabbar/store.ts` 已把 `tabbarList` 从 `reactive` 数组改成角色过滤的 `computed`，代码大量使用 `tabbarList.value`；当前项目仍是普通 `reactive` 数组。
- 上游 `setCurIdx` 没有当前项目的登录态和白名单/黑名单 guard；当前项目的 `tokenStore.hasLogin`、`isNeedLoginMode`、`judgeIsExcludePath` 必须保留。
- 上游角色 tabbar 已经和最终 store 结构耦合；本轮只抽同步逻辑，不引入角色 tabbar。
- 上游 `src/tabbar/index.vue` 已抽出 `TabbarItem.vue`，当前项目仍是单文件实现；因此是重写/移植思路，不是套 diff。
- `03e115f` 中的 router interceptor 改动在 `packages/cli/features/login/files/src/router/interceptor.ts`，是 CLI 模板文件，不是当前 runtime。

需要提取的思路：

- `src/tabbar/store.ts`
  - 新增 `normalizeRoutePath(path?: string)`。
  - `isPageTabbar()` 使用 normalize 后路径，并把 `/` 视为首页 tabbar。
  - 新增 `findTabbarIndexByPath()` 和从页面栈倒序寻找最近 tabbar 页的逻辑。
  - `setAutoCurIdx()` 找不到当前路径时，回退到页面栈里的最近 tabbar。
  - 新增 `syncCurIdxByCurrentPage()` / `syncCurIdxByCurrentPageAsync()`。
  - 新增 `isCurrentRouteTabbarItem(index)`。
- `src/tabbar/index.vue`
  - 点击当前 tab 时，只有“当前高亮且当前路由就是该 tab”才阻止跳转。
  - `switchTab` / `navigateTo` 完成后调用同步。
- `src/App.vue`、`src/App.ku.vue`
  - `onShow` 后同步当前 tabbar index。
  - H5 下监听 `visibilitychange` / `pageshow` 后同步 tabbar。

验证：

- H5：工作台、审批、通讯录、消息、我的之间连续切换，高亮不乱。
- H5：进入非 tabbar 子页面后返回，高亮恢复到最近的 tabbar 页。
- H5：浏览器 tab 切走再切回，高亮仍正确。
- 微信小程序：`switchTab` 后高亮正确；分享/直达路径进入时高亮正确。
- 未登录点击需要登录的 tab 时，当前登录拦截逻辑仍然生效。
