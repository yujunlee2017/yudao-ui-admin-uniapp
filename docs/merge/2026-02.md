# 2026-02 合并计划

## 月度结论

2026-02 噪音最多。核心只看 `b9ef57a5` 的运行时和 dev-tools 价值；不要引入 monorepo CLI、demo、模板文档和上游版本号。

## commit 分类

| 类型 | commit | 处理建议 |
| --- | --- | --- |
| 完全合并 | 无 | 没有适合完整 cherry-pick 的 commit。 |
| 部分合并 | `b9ef57a5` | 作为统一提取包：token 响应式、`SKIP_OPEN_DEVTOOLS`、dev/build 输出目录、小程序上传脚本可选。 |
| 部分合并 | `a2581d2`、`658dd12` | 依赖升级，需测试。 |
| 部分合并 | `f6f4e5b`、`46ca406` | dev 相关修复，只按现状需要抽取。 |
| 忽略 | `44a4851` | 上游 CI。 |
| 忽略 | `7166084`、`16a8aa1`、`4011617`、`b0f87d5`、`bcfe18d`、`c583874`、`bc3ac30` | 上游合并提交。 |
| 忽略 | `9ca2f14`、`c98e4fa`、`47cb7df`、`27a1082`、`c43d1c4` | 上游文档/引用。 |
| 忽略 | `e655dc1`、`ff62c99`、`2391c96`、`45147a3`、`0ea0870`、`871e790`、`773fc63`、`5665a60` | CLI 相关。 |
| 忽略 | `810afe4`、`e1c6bfb`、`6e88173`、`bd5f436`、`a51ce08`、`db07f5a` | 图表/示例功能，当前不需要。 |
| 忽略 | `24e6331` | TDesign CLI 支持。 |
| 忽略 | `98b2381`、`9f7285f` | 格式化。 |
| 忽略 | `1a34f86`、`b9e8709`、`25e8d95` | 上游版本号。 |
| 忽略 | `d2837cd` | npm publish。 |
| 忽略 | `80c50e7` | i18n 脚手架。 |
| 忽略 | `d0efe49` | lock/html 噪音。 |

## `b9ef57a5` 统一提取包

不要拆到多个批次。建议一个分支内处理，按功能点提交。

### token 响应式

需要提取：

- `src/store/token.ts`
  - 增加 `nowTime = ref(Date.now())`。
  - 增加 `updateNowTime()`，内部更新 `nowTime.value`，并返回 `useTokenStore()`。
  - `isTokenExpired` 使用 `nowTime.value`，不要继续直接用 `Date.now()`。
  - 不要把上游对 `isRefreshTokenExpired` 的 `nowTime.value` / `refreshTokenExpireTime` 改动照搬过来。
  - 在 `setTokenInfo()` 开头调用 `updateNowTime()`。
  - 在 `refreshToken()` 的 `finally` 中调用 `updateNowTime()`。
  - 在 `logout()` 的 `finally` 中调用 `updateNowTime()`。
  - 在 `tryGetValidToken()` 开头调用 `updateNowTime()`。
  - 返回对象中暴露 `updateNowTime`。
- `src/http/interceptor.ts`
  - 取请求头 token 时，从 `tokenStore.validToken` 改为 `tokenStore.updateNowTime().validToken`。

明确决策：refresh token 过期逻辑不从上游照搬。

- 当前后端不返回 refresh token 过期时间，所以不要恢复 `refreshTokenExpireTime` 本地存储。
- 双 token 模式下，`isRefreshTokenExpired` 继续按当前项目逻辑返回 `false`。
- refresh token 最终以刷新接口失败或 401 作为失效依据。
- `nowTime` 只用于 access token 过期判断。

### dev-tools

需要提取：

- `scripts/open-dev-tools.js`
  - `_openDevTools(env = 'dev')` 支持 `dev` / `build` 输出目录。
  - 根据 `env` 选择 `dist/dev/${UNI_PLATFORM}` 或 `dist/build/${UNI_PLATFORM}`。
- `vite.config.ts`
  - 从 `process.env` 读取 `SKIP_OPEN_DEVTOOLS`。
  - `openDevTools({ mode })` 只在 `SKIP_OPEN_DEVTOOLS !== 'true'` 时启用。

如果已经提取过 [2026-01](./2026-01.md) 的 `c342a3f`，这里要按功能去重。

### 小程序上传脚本（可选）

- `scripts/upload-weixin.js`
- `package.json`
  - 增加 `upload:mp`。
  - 增加 `miniprogram-ci`。

只有明确需要 CLI 上传时才引入。

## 不要引入

- `packages/cli/**`
- `pnpm-workspace.yaml`
- `workspaces`
- 上游 demo 页面
- 上游 README / AGENTS / 模板文档

## 验证要求

- 单 token：登录成功后请求带 token；手动把 `accessTokenExpireTime` 改成过去时间后，`validToken` 应为空。
- 双 token：手动把 `accessTokenExpireTime` 改成过去时间后，请求触发 refresh；刷新成功后重放请求；刷新失败后清理登录态并跳转登录页。
- `SKIP_OPEN_DEVTOOLS=true pnpm build:mp:prod` 不自动打开开发者工具。
- `pnpm type-check`
