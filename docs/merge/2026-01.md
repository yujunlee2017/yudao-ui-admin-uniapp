# 2026-01 合并计划

## 月度结论

2026-01 主要关注 dev-tools 输出目录选择和 router 小修。整体改动少，但仍建议按功能点提取。

## commit 分类

| 类型 | commit | 处理建议 |
| --- | --- | --- |
| 完全合并 | `c342a3f` | 开发者工具按 dev/build 输出目录打开，目标清晰。若 `b9ef57a5` 已经提取同类逻辑，则不要重复处理。 |
| 部分合并 | `e8ae13d` | 路由不存在判断修复，需适配当前 `src/router/interceptor.ts`，不能直接覆盖。 |
| 部分合并 | `f239744` | router 注释清理，只按需吸收。 |
| 忽略 | `01fc19a`、`b111eb8` | 上游版本号更新。 |

## 代码提取点

### `c342a3f`

- `scripts/open-dev-tools.js`
  - 根据构建模式选择 `dist/dev/${UNI_PLATFORM}` 或 `dist/build/${UNI_PLATFORM}`。
- `vite.config.ts`
  - 调用 `openDevTools({ mode })`。

注意：如果后续用 [2026-02](./2026-02.md) 的 `b9ef57a5` 统一提取包处理 dev-tools，这个改动会被覆盖，不要重复提交。

## 验证要求

- `pnpm dev:mp-weixin` 后打开 `dist/dev/mp-weixin`。
- `pnpm build:mp:prod` 后如需打开，应指向 `dist/build/mp-weixin`。
- H5 不受影响。
